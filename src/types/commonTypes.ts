export enum SupportedLanguages {
  en = 'en',
  zh = 'zh',
}

export enum AuctionStatus {
  ready = 'READY',
  live = 'LIVE',
  past = 'PAST',
}

export enum LotStatus {
  ready = 'READY',
  live = 'LIVE',
  past = 'PAST',
}
