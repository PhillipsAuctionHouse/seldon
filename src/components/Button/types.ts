export enum ButtonVariants {
  primary = 'primary',
  secondary = 'secondary',
  tertiary = 'tertiary',
  destructive = 'destructive',
  link = 'link',
}

export enum ButtonSizes {
  default = 'default',
  small = 'small',
}
