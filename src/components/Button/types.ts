export enum ButtonVariants {
  primary = 'primary',
  secondary = 'secondary',
  ghost = 'ghost',
  tertiary = 'tertiary',
  link = 'link',
}

export enum ButtonSizes {
  default = 'default',
  small = 'small',
}
